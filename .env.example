# =============================================================================
# Environment variables for whereami-text2sgm
#
# Copy this file to .env and fill in your value:
#   cp .env.example .env
#
# Loaded automatically at startup via python-dotenv.
# =============================================================================

# OpenAI API key — required only for:
#   - single_inference.py (GPT-based text-to-graph parsing)
#   - visualize_loc_from_query.py (same)
#   - Ada embedding backend (graph.embedding_type=ada)
OPENAI_API_KEY=
